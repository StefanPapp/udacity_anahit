apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    service: sentiments-analyzer
  name: sentiments-analyzer
spec:
  replicas: 2
  selector:
    matchLabels:
      service: sentiments-analyzer
  template:
    metadata:
      labels:
        service: sentiments-analyzer
    spec:
      containers:
      - image: stefanpapp/sentiments-analyzer
        name: sentiments-analyzer
        imagePullPolicy: Always
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "1024Mi"
            cpu: "500m"
        ports:
        - containerPort: 8080
      restartPolicy: Always
